export declare const EV_ONLY_NORMALIZED_PATHS: Array<{
    category: string;
    key: string;
    label: string;
}>;
export declare const EV_ONLY_RAW_KEYS: Array<{
    key: string;
    label: string;
}>;
export declare const ICE_ONLY_NORMALIZED_PATHS: Array<{
    category: string;
    key: string;
    label: string;
}>;
export declare function isElectricFuelType(name: string): boolean;
export declare function isHybridFuelType(name: string): boolean;
